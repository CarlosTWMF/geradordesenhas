<!DOCTYPE html>
<html lang="en">
<head>
    <% include head %>
</head>
<body class="container">

    <header>
        <% include header %>
    </header>

     <main>
        <div class="row">
            <div class="col-sm-8">
                <div class="jumbotron">
                    <h2>Qual o tamanho que sua senha deve ter?*</h2>
                    <br />
                    <br />
                     <form action="/calcular" method="post">
                     	<input type="hidden" name="operacao" value="<%- operacao %>" />
							<div class="input-group col-lg-6" >
								<input type="text" class="form-control" placeholder="32" name="numero1" >
								<span class="input-group-btn">
								<button class="btn btn-default" type="submit">Gerar Senha</button>
								</span>
							</div><!-- /input-group -->
					</form>
					<br />
					<br />
					<br />
					<p class="warning">* Se você não especificar, será gerada uma senha de 32 caracteres.</p>
                </div>
                
            </div>
            <div class="col-sm-4">
                
                <div class="well">
                    <% if(typeof(resultado) != 'undefined') { %>
						<h3>Esta é a sua senha</h3>
						<p><%= resultado %></p>
						<!--<input type="text" name="numero1" value="<%= resultado %>" />-->
					<% } %>
                </div>

            </div>
        </div>
    </main>

    <footer>
        <% include footer %>
    </footer>
    
</body>
</html>


